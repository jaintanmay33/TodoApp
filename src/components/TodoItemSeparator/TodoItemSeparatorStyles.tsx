import {StyleSheet} from 'react-native';
import {COLORS, APP_SIZES} from '../../utilities/constants';

const todoItemSeparatorStyles = StyleSheet.create({
  container: {
    padding: APP_SIZES.size10,
  },
});

export default todoItemSeparatorStyles;
